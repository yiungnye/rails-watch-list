<%= render "layouts/navbar" %>
<div class="container">
  <div class="my-list">
  <h3>My List</h3>
  <p class="btn"><%= link_to 'Create a movie list' %></p>
  </div>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
</div>
